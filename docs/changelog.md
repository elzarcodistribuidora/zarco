# Mejoras ya hechas

- ✅ Imágenes WebP (-92%).
- ✅ Backend en **Supabase** (Postgres + Auth + RLS), Apps Script retirado.
- ✅ **Panel admin** (CRUD completo: visualización, edición y **creación** de productos; gestión de clientes/pedidos/cotizaciones) — reemplaza el Sheet.
- ✅ Seguridad real por fila (RLS): cada cliente solo ve lo suyo.
- ✅ Login con Google (popup) + menú de perfil flotante (sin pantalla azul).
- ✅ Captura de **leads** de prospectos (form de contacto → `/api/quote`).
- ✅ Catálogo cacheado (ISR) + revalidación on-demand al editar precios.
- ✅ SEO por página + sitemap + robots.
- ✅ **Rediseño Premium en Delicatessen**: Animación nativa en navbar, sección de charolas compacta y de lujo con **ilustraciones SVG a todo color**.
- ✅ **Banners WebP y Multi-Slider**: Conversión de todos los nuevos banners (escritorio y móvil) a formato WebP, inyección del motor de carrusel (slider) en Delicatessen, y sincronización cruzada de banners promocionales con la página de inicio.
- ✅ **Banner promocional en Home**: Inyección de banner adaptativo (escritorio/móvil) en la página principal, vinculado dinámicamente a la sección de charolas en Delicatessen, posicionado estratégicamente entre familias y líderes.
- ✅ **Fix de Navbars (Móvil y Transiciones)**: Se reestructuró `DelicatessenNavbar` para que sea idéntica en estructura y funcionalidad a la navbar normal en móvil (incluyendo el módulo de autenticación). Se solucionó un bug severo en `PageScripts.tsx` que acumulaba listeners de `DOMContentLoaded` en cada transición de ruta en Next.js, lo que impedía que el menú hamburguesa funcionara al cambiar de página.
- ✅ **Formulario "Arma tu Charola"**: Creada una experiencia interactiva tipo Typeform en `/delicatessen/arma-tu-charola` para cotizar tablas de quesos y carnes frías. Incluye rediseño 100% responsivo, manejo de estado complejo (selección de tamaño, ingredientes con medidas, extras, notas) y generación de cotizaciones estructuradas enviadas directamente a WhatsApp. Redirecciones dinámicas agregadas en todo el sitio (Index y Delicatessen) hacia esta nueva experiencia.
- ✅ **Limpieza de la raíz del repo (jul 2026)**: se borraron ~35 scripts de un solo uso (`patch_*`, `fix_*`, `test_*`, `convert_*`, `update_*`) que se habían acumulado para parchear `src/webflow/*.json` a mano, más logs/output sueltos. Herramientas independientes (generador de QR, scrapers) se movieron a `tools/`.
- ✅ **Migración completa del sitio público a JSX + Tailwind (jul 2026)**: las 15 páginas que eran blobs de HTML/CSS/JS de Webflow (`src/webflow/*.json` + catch-all `[[...slug]]/page.tsx`) se reescribieron como componentes React reales, con una librería de componentes compartidos (`Navbar`, `Footer`, `SectorPage`, `ProductCarousel`, `HeroSlider`, `Marquee`, `GuideAccordion`, `FaqAccordion`, `ContactForm`, `LegalPage`, y el motor completo de `/catalogo` con carrito/checkout). El catch-all se eliminó. Detalle en `docs/marketing-site.md`.
- ✅ **Fixes de navbar post-migración**: subrayado nativo de `<a>`, fondo/borde nativo de `<button>` (sin preflight de Tailwind hacían falta resets puntuales), centrado del dropdown de "Productos" (se posicionaba mal por un `relative` de más en un contenedor intermedio), grosor del dropdown, y el efecto óptico de texto en mayúsculas "pegado arriba" en el line-height (se igualó con `leading-none` en todos los links del nav).
- ✅ **Migración de `/perfil` (jul 2026)**: `/perfil` pasó a usar el `Navbar`/`Footer` compartidos; su dashboard (`PerfilDashboard.tsx`, sin cambios) ahora usa un CSS propio (`perfil/perfil.css`) extraído una sola vez del `perfil.json` viejo. Con esto se borró `src/webflow/` entero (incluyendo `_pages.json` — `sitemap.ts` ahora usa un arreglo de slugs hardcodeado — y `perfil.json`), `webflow-export/`, y los scripts `mirror-webflow.mjs`/`optimize-images.mjs`/`build-pages.mjs` junto con sus comandos en `package.json`.
- ✅ **Navbar/Footer de Delicatessen reconstruidos y `PageScripts.tsx`/`webflow-shared.css` retirados (jul 2026)**: `DelicatessenNavbar`/`DelicatessenFooter` ya no son HTML/CSS crudo de Webflow — son el mismo `Navbar`/`Footer` compartido (misma lógica de scroll/drawer/auth/`--navbar-h`), recoloreado a gris oxford (`#3A3D42`) + rojo Zarco (`#A81200`), con el GIF promocional normal en el centro. Al ya no depender de las clases legacy (`.nav-link`, `.navbar`, etc.), se pudo borrar `PageScripts.tsx` por completo (su único uso restante era `/delicatessen/arma-tu-charola`, y el login ahí ya lo maneja `useZarcoAuth` como en el resto del sitio) y quitar el `<link>` a `webflow-shared.css` del layout — ya no lo usa nada.
- ✅ **Barrido de resets globales sin preflight (jul 2026)**: al no cargar el preflight completo de Tailwind (para no romper páginas Webflow crudas que ya no existen), varios comportamientos nativos del navegador se colaban y hubo que resetearlos a mano en `marketing-tailwind.css`: `<a>` subrayado, `<button>` con fondo/borde nativo, `<ul>/<li>` con viñetas, y — el más notorio — **`<body>` con el `margin: 8px` por defecto**, que se veía como un marco blanco alrededor de **toda** la página en todas las rutas (no era un problema de los banners). También se fijó `font-family: Inter` en `body` (antes cada componente tenía que declararla a mano, y el texto sin estilo explícito caía en la fuente serif por defecto del navegador).
- ✅ **Navbar: ocultar al bajar / reaparecer chico al subir + `--navbar-h`**: el navbar ahora se esconde (`-translate-y-full`) al bajar scroll y reaparece encogido al subir, hasta llegar arriba del todo donde recupera su tamaño completo. Además mide su propia altura real con `ResizeObserver` y la publica como variable CSS `--navbar-h` — las 9 páginas que antes tenían un `padding-top` adivinado a mano para no quedar tapadas por el navbar fijo ahora usan `pt-[var(--navbar-h)]` (o `calc(var(--navbar-h) + Npx)` donde hacía falta aire extra), así que ya no hay que recalcular píxeles cada vez que se ajusta el navbar.
- ✅ **Banners a todo el ancho, sin redondear, en las 9 páginas que tienen hero banner** (antes solo se había corregido en Home): se quitó el patrón `w-[90%] lg:rounded-3xl` que quedaba en `SectorPage`, `nosotros`, `delicatessen` y `catalogo`.
- ✅ **Marquee de marcas restaurado a como era antes**: las imágenes de marca ya venían diseñadas como insignias circulares (navy + aro rojo) — el componente `Marquee.tsx` les aplicaba `grayscale`/`opacity-70`, lo cual las apagaba. Se quitó ese filtro y se agrandaron (`h-24 lg:h-32`) para que se vean a full color como en el diseño original.
- ✅ **Home fiel al sitio original (jul 2026)**: se comparó `elzarcodistribuidora.com.mx` (aún no migrado en ese momento) contra el nuevo `/` para corregir 3 secciones que se habían reconstruido con datos/estilos aproximados en vez de los reales:
  - **"Nuestras Líneas de Negocio"**: las 3 tarjetas (Restaurantes/Tiendas/Cafeterías) tenían imágenes equivocadas (banners de cremería/embutidos reciclados) porque el fondo original venía por CSS `background-image`, no por un `<img>` visible en el HTML que se había extraído. Se recuperaron las imágenes reales (`Diseno-sin-titulo*.webp`, ya estaban en `public/assets/`) inspeccionando el CSS de producción, y se igualó el radio (12px), alto (500px) y el botón "Ver Selección" (oculto por defecto, aparece como pill blanco→rojo solo al hacer hover de la tarjeta).
  - **"Familias de Productos"**: se quitó la tarjeta blanca con borde que envolvía cada imagen — el diseño original no tiene tarjeta, son solo las imágenes circulares (ya traen el círculo rojo integrado en el asset) con `drop-shadow`, y el título se pone rojo únicamente al hover.
  - **"Líderes que Confían en Nuestra Red"**: mismo caso — se quitó el envoltorio con fondo blanco/borde/padding que no existía en el original; los banners van directos, sin caja, con `border-radius: 10px` y scale al hover.
- ✅ **Navbar más delgado (3 rondas) + sin viñetas**: se redujo el padding vertical de la barra de links tres veces (`py-4`→`py-2`→`pt-[11px] pb-[6px]`→`py-[5px] lg:py-1` en el contenedor + `py-3`→`py-1.5` en cada link/trigger) hasta que el texto se viera parejo y centrado, y se agregó `list-style: none` global a `marketing-tailwind.css` (los `<li>` traían viñeta nativa del navegador sin el preflight de Tailwind). Aplicado en `Navbar.tsx` y `DelicatessenNavbar.tsx` (misma estructura, recoloreada).
- ✅ **Fixes del carrito flotante de `/catalogo` (jul 2026)**: el botón flotante del carrito pasó de estar a la derecha (chocando en la misma esquina que el botón de WhatsApp del footer) a quedar centrado abajo con `z-[10000]` (por encima de todo). Se quitó la barra "Te faltan $X para envío sin costo" del drawer del carrito. Se corrigió un bug real: al `<aside>` del drawer le faltaba `id="cartDrawer"` — sin ese id, `CatalogRecs.tsx` (el cross-sell "Completa tu pedido") nunca encontraba el contenedor y no se montaba nunca en el sitio ya migrado a JSX. También se ocultó el botón flotante de WhatsApp del footer específicamente en `/catalogo` (vía `usePathname` en `Footer.tsx`), ya que esa página tiene su propio botón flotante de carrito en la misma zona.
- ✅ **Subnav de anclas → pill flotante ("estilo Zarco")**: la barra de secciones (`SectorPage`, Delicatessen) dejó de ser una línea plana con texto — ahora es una cápsula blanca con sombra, cada sección es su propia píldora, la primera activa en rojo Zarco sólido y el resto en navy suave que se pinta de rojo claro al hover.
- ✅ **`ProductCarousel` rediseñado y a todo lo ancho**: las tarjetas de producto se rehicieron (imagen más grande sobre fondo degradado, marca como "eyebrow" en rojo, botón circular navy→rojo al hover, sin animación de levantar/brillar la tarjeta completa por pedido explícito) y se agrandaron (`230px`→`270px`). Se corrigió que el carrusel quedaba encajonado por el contenedor centrado de `90%`/`1300px` — ahora solo el título/flechas viven en ese ancho, la fila de tarjetas es `w-full` real y se extiende hasta el borde de la pantalla (alineada a la izquierda con el título vía `pl-[5%]`), así la última tarjeta ya no se corta a media pantalla en monitores anchos.
- ✅ **Sección de beneficios rediseñada (sin íconos SVG genéricos)**: en `SectorPage` y Delicatessen, los íconos en cuadros rojos se reemplazaron por un tratamiento editorial — número grande (`01`/`02`/`03`) en rojo muy tenue de fondo, con el título superpuesto encima y separadores verticales finos entre columnas. También se pasó el fondo de esas páginas de gris (`#F4F7F9`) a blanco puro.
- ✅ **CTA de WhatsApp fusionado con el footer (`WhatsappCTA.tsx`)**: el banner de cierre ("¿Buscas mejor precio...?") dejó de ser una tarjeta redondeada flotando con espacio antes del footer — ahora es un componente compartido, delgado, full-width, pegado directo arriba del `Footer` (se leen como una sola sección continua). Layout horizontal (pregunta a la izquierda, botón pill a la derecha con flecha en círculo animada al hover). El botón ya no abre WhatsApp directo (ya está el botón flotante para eso) — enlaza a `/contacto`. Tiene variante de color: navy con acento rojo en el resto del sitio, rojo con acento gris en Delicatessen.
- ✅ **Rediseño editorial de `/nosotros`, `/guias-de-negocio` y `/contacto` + GSAP + SEO (jul 2026)**: las 3 páginas perdieron el patrón de "tarjeta" (borde + esquinas redondeadas + sombra envolviendo bloques) a favor del mismo lenguaje editorial de `SectorPage`/Delicatessen — separadores finos, números grandes como índice, sin cajas. Detalle completo en `docs/marketing-site.md`, resumen:
  - **`/nosotros`**: "Nuestras Raíces" y "Dominio Logístico" en secciones independientes; números de año **sólidos** en rojo (no tenues — corregido tras feedback directo); stats de autoridad (alcaldías cubiertas / transporte propio / intermediarios) junto al CTA; píldoras de cobertura en una sola fila de ancho completo; `FaqAccordion` con numeración en badge circular; fondo blanco puro en toda la página (antes alternaba con `#F4F7F9`, se veía como una costura entre secciones).
  - **`/guias-de-negocio`**: `GuideAccordion` pasó de acordeón-tarjeta a lista editorial; sus CTA ahora capturan lead en `/api/quote` (antes iban directo a `wa.me` sin registrar nada) y personalizan el mensaje de WhatsApp con el título de la guía; se corrigió el sidebar `sticky` (un `Reveal` con `transform` en el ancestro lo rompía) y se agregó `<h1>` + título de metadata con marca (antes "Guías de Negocio", 16 caracteres, sin marca).
  - **`/contacto`**: renombrado de "Ventas y Logística"/"Atención a Negocios" a **"Solicita tu Cotización"** (h1 + metadata) para dejar claro que la página es para cotizar. `ContactForm.tsx` rediseñado (inputs con ícono, selector de zona restilizado pero sigue siendo `<select>`, validación custom en vez del tooltip nativo del navegador que se montaba encima del label). Se corrigió además un bug de layout (`min-w-0` faltante causaba que los inputs de "Nombre"/"Razón Social" se desbordaran uno sobre otro en el grid) y uno de tipografía sitewide (`<textarea>` caía en fuente monoespaciada por no heredar `font-family` sin el preflight de Tailwind).
  - Se agregó **GSAP** (`gsap` + `@gsap/react`) para animaciones de scroll (parallax, stagger, pop-in) en estas 3 páginas, y **JSON-LD** (`Organization`+`FAQPage` en `/nosotros`, `CollectionPage` en `/guias-de-negocio`, `LocalBusiness` en `/contacto`) — el sitio no tenía datos estructurados en ninguna página antes de esto.
  - Se instalaron ~33 skills de marketing (SEO, copywriting, CRO, psicología de marketing) y las skills oficiales de GSAP como skills de proyecto (`.claude/skills/`), usadas para auditar y priorizar estas mejoras.
- ✅ **Rediseño visual del catálogo "estilo Zarco" (jul 2026)**: `CatalogApp.tsx` (barra de filtros, tabla, carrito) pasó de un look genérico a la identidad navy/rojo del resto del sitio — filtros en tarjeta blanca con captions editoriales, header de tabla en navy, badges de categoría a color, precio en rojo bold, filas agregadas en verde, drawer del carrito con header degradado. `CatalogRecs.tsx` (recomendaciones cross-sell/upsell) también se pulió: toda la tarjeta es clickeable (no solo el botón) y el estado "agregado" ahora tiene un badge de palomita real en vez de solo cambiar el texto del botón. Se corrigieron dos bugs reales en el proceso: el botón flotante del carrito quedaba tapado por el botón flotante de WhatsApp del footer (misma esquina, `z-[9999]`) — se veía como si "el carrito no apareciera"; y el buscador mostraba el borde/recuadro nativo del navegador porque dos reglas CSS con el mismo grupo de selectores (`input, textarea, select` en distinto orden) hacían que Lightning CSS descartara la segunda por duplicada. Detalle completo en `docs/marketing-site.md`.
- ✅ **`/catalogo` sin preloader de página completa (jul 2026)**: se borró `Preloader.tsx`/`preloader.css` (logo + barra de progreso 0→100%, montado solo en `catalogo/page.tsx`) — mientras el inventario carga, la tabla ahora muestra filas skeleton animadas directo en su lugar, sin bloquear el resto de la página.
- ✅ **Rediseño editorial de `/aviso-de-privacidad` y `/terminos-del-servicio` (jul 2026)**: `LegalPage.tsx` perdió el patrón de tarjeta (cada sección era una caja blanca con borde/sombra + ícono SVG en cuadro rojo) a favor del mismo lenguaje editorial del resto del sitio — número grande `01/02/03` en rojo tenue, separadores finos entre secciones, items con guion rojo en vez de check sobre caja gris. Se quitaron los 8 SVG decorativos de ambas páginas y el badge/pill superior ("Tus datos, tu negocio" / "Reglas claras, negocios largos"). El sidebar de anclas ("¿Qué buscas?") ahora es genuinamente `sticky` (antes tenía el CSS pero no `lg:self-start`) y cada sección tiene `scroll-mt` para no quedar tapada por el navbar fijo al hacer clic en un ancla.
- ✅ **Footer: links legales reposicionados (jul 2026)**: "Términos de Servicio" y "Aviso de Privacidad" se movieron de estar pegados al copyright (izquierda) a su propia fila centrada y en texto más chico, debajo de "Powered by Flouvia" — mismo cambio en `Footer.tsx` y `DelicatessenFooter.tsx`.
- ✅ **`/perfil` reescrito en Tailwind (jul 2026)**: `PerfilDashboard.tsx` dejó de depender de `perfil/perfil.css` (239 líneas de CSS legacy extraído del `perfil.json` de Webflow, ya borrado) y de su bloque `<style dangerouslySetInnerHTML>` propio — ahora es Tailwind puro con el mismo lenguaje editorial del resto del sitio: sin tarjetas (`.card`/`.metric-box` fuera), KPIs como fila de estadísticas con divisores verticales, historial de pedidos con toggle `+`→`×` circular (mismo patrón que `FaqAccordion`), avatar como círculo rojo con iniciales, skeleton con `animate-pulse`. Se quitaron los íconos SVG decorativos, conservando solo los de marca (WhatsApp, Google). Ningún cambio funcional: fetch a `/api/portal`, cargar al carrito, repetir pedido, descargar nota (PDF), búsqueda, login/logout funcionan igual que antes.
- ✅ **Banner de "Arma tu Charola": nuevo en Home, el original se queda solo en su página (jul 2026)**: el banner grande de la mesa de charolas (`charolas-desk.png`/`charolas-movil.png`) vive ahora exclusivamente en `/delicatessen/arma-tu-charola`, pegado al navbar. En Home se puso un banner promocional nuevo (`charolas-promo-desk.webp`/`charolas-promo-movil.webp`, ~50 KB c/u tras convertir a WebP) que enlaza ahí mismo, con una pill "Arma tu Charola" que aparece al hacer hover sobre un overlay oscuro. De paso se corrigió un bug de **scroll horizontal** en el banner de `arma-tu-charola`: el `<section>` usaba `width:'100%'` + `padding: 5%` por estilos inline sin `box-sizing: border-box` (el sitio no carga el preflight de Tailwind), así que el padding se sumaba al 100% del ancho. Se resolvió adoptando el mismo patrón full-bleed sin padding lateral que usan los demás banners del sitio.
- ✅ **"Arma tu Charola" (`TrayBuilder.tsx`) más Typeform: teclado completo + rediseño minimalista (jul 2026)**: `Enter` avanza en cualquier paso (antes solo en la intro), flechas `↑`/`↓` navegan, dígitos `1`-`4` seleccionan tamaño directo, `Shift+Enter` en notas inserta salto de línea en vez de avanzar. Las tarjetas de ingredientes pasaron de checkbox-en-caja a filas con subrayado + insignia circular con palomita al seleccionar; el textarea de notas pasó a subrayado simple. Se corrigieron dos bugs reales: (1) si clickeabas un ingrediente y luego dabas Enter para avanzar, el navegador también disparaba un click nativo sobre ese botón enfocado, deseleccionando el ingrediente justo al avanzar — se previene con `preventDefault` cuando el target es un `BUTTON`; (2) avanzar/retroceder usaba `window.scrollTo(0,0)`, que peleaba contra el estado interno de **Lenis** (scroll suave del sitio) y terminaba regresando hasta el banner de arriba, sintiéndose como una recarga de página — `SmoothScroll.tsx` ahora expone la instancia de Lenis en `window.__lenis` para que `TrayBuilder` la use directamente y el scroll quede sincronizado.
- ✅ **Ícono de WhatsApp deformado, corregido en todo el sitio (jul 2026)**: el glyph de WhatsApp (`viewBox="0 0 448 512"`, proporción no cuadrada) se forzaba a cajas cuadradas de `22×22px` en `Footer.tsx`, `DelicatessenFooter.tsx` y `TrayBuilder.tsx`, aplastándolo en un óvalo en vez de un círculo — se corrigió el ancho a `19px` en los 4 lugares para respetar la proporción real. De paso se encontró y borró un botón flotante de WhatsApp **duplicado** en `delicatessen/page.tsx` (ya lo traía `DelicatessenFooter.tsx` en la misma posición — quedaban apilados, invisible pero código muerto).
- ✅ **Botones CTA de navbar → pill "estilo Zarco" (jul 2026)**: el botón de acción de `Navbar.tsx` ("Cotizar") y `DelicatessenNavbar.tsx` ("Arma tu Charola") dejaron de ser un botón rectangular sólido para adoptar la misma pill (texto + círculo con flecha) que ya usaba `WhatsappCTA.tsx`, adaptada de color a cada navbar. El botón "Cotizar mi Charola Ahora" de `/delicatessen` ahora enlaza a `/delicatessen/arma-tu-charola` (antes iba directo a `wa.me` sin pasar por el formulario).
- ✅ **Sección "Charolas & Tablas Premium" de `/delicatessen` rediseñada dos veces (jul 2026)**: las 3 tarjetas con borde/sombra y SVGs decorativos dibujados a mano no convencían visualmente. Primer intento: mismo patrón de número tenue `01/02/03` que ya usa `SectorPage` — descartado a pedido explícito del usuario por repetir un recurso que ya existe en el resto del sitio. Diseño final: lista tipo **menú de restaurante**, cada tier como fila completa (nombre / descripción / pill de capacidad), separadas por líneas finas, sin tarjeta envolvente (ya no vive dentro de la caja blanca `rounded-3xl shadow` que tenía antes — está directo sobre el fondo de `<main>`). La fila "Charola Premium" lleva franja roja + ícono de estrella SVG (antes usaba el carácter ★ y un pill de fondo rojo sólido). Toda la fila es clickeable (`<Link>` a `/delicatessen/arma-tu-charola`) con flecha circular que aparece al hover.
- ✅ **3 bugs reales de móvil corregidos, sitio verificado en viewport móvil con Playwright (jul 2026)**: en móvil el sitio parecía mostrar "solo la navbar" — la causa real era el **drawer del menú móvil siempre abierto**: `Navbar.tsx`/`DelicatessenNavbar.tsx` tenían la clase `right-0` fija en la base del `<div>` del drawer, además de la condicional `drawerOpen ? "right-0" : "-right-full"` — ambas clases con la misma especificidad, y `right-0` siempre ganaba, dejando el drawer fijo y visible tapando todo el contenido. Se quitó el `right-0` de la base. De paso se corrigieron dos bugs más encontrados en la misma revisión: el **GIF promocional del navbar** (`GIF-EL-ZARCO-1.webp`, 700×150px) se forzaba a `h-[100px]` en todas las pantallas — en móvil eso da ~467px de ancho, más que el viewport, y el texto ("TODO PARA TU NEGOCIO") quedaba cortado; ahora usa `h-[65px] lg:h-[100px]` (`h-[38px] lg:h-[60px]` con scroll) + `max-w-full`. Y **`/contacto` tenía scroll horizontal en móvil**: los inputs de `ContactForm.tsx` combinan `w-full` + `padding` + `border` en el mismo elemento, pero sin el preflight de Tailwind `input`/`textarea`/`select` no tenían `box-sizing: border-box` — se agregó a la regla de reset que ya existía para esos elementos en `marketing-tailwind.css` (mismo patrón puntual del proyecto, sin duplicar el grupo de selectores). Verificado con Playwright headless en viewport 390×844: las 17 páginas públicas sin overflow horizontal.
- ✅ **Home: 3 secciones a carrusel horizontal solo en móvil (jul 2026)**: "Nuestras Líneas de Negocio", "Familias de Productos" y "Líderes Que Confían en Nuestra Red" pasaron de un `grid` de una columna en móvil (todo apilado verticalmente) a un carrusel horizontal con **peek** (la primera tarjeta/logo se ve completa y la segunda cortada, para que se note que hay más), usando el mismo patrón de `overflow-x-auto` + `snap-x snap-mandatory` + scrollbar oculta que ya usaba `ProductCarousel.tsx`. En `sm`/`md` (donde antes empezaba el grid) revierte exactamente al layout de grid de siempre — desktop no cambió. De paso, `Navbar.tsx`/`DelicatessenNavbar.tsx` ganaron `overflow-x-hidden` en el `<header>` como red de seguridad para que la barra nunca se sienta inestable horizontalmente en pantallas angostas.
- ✅ **Drawer móvil rediseñado (transform + panel angosto "más pro") y 3 bugs reales más (jul 2026)**: el drawer del menú móvil animaba la propiedad `right` (recalcula layout en cada frame, se sentía tembloroso) — se cambió a `transform: translateX` (acelerado por GPU). También pasó de ocupar casi toda la pantalla (`w-[85%]`) a un panel angosto (`w-[74%] max-w-[320px]`, `rounded-l-[28px]`) que deja ver el overlay oscuro a la izquierda. Se corrigió que "Inicio" en el drawer móvil de Delicatessen apuntara a `/delicatessen` en vez de a `/` (inconsistente con escritorio). Se encontró y corrigió un **bug real que rompía el dropdown de "Productos" en escritorio** (regresión de la sesión anterior): el `overflow-x-hidden` agregado al `<header>` completo forzaba —por una regla del spec de CSS Overflow, no evitable ni con `overflow-y-visible` explícito— que `overflow-y` se computara como `auto`, recortando el dropdown que sobresale por debajo de la barra de links; se resolvió acotando el `overflow-x-hidden` solo a la barra del logo/GIF. Se centraron las flechas ‹ › de navegación de `TrayBuilder.tsx` (`/delicatessen/arma-tu-charola`) en móvil — quedaban tapadas por el botón flotante de WhatsApp en la misma esquina, mismo patrón de bug que ya se había corregido en el carrito de `/catalogo`. Se actualizó el banner móvil de esa misma página con una imagen nueva provista por el usuario. Detalle completo en `docs/marketing-site.md`.
- ✅ **2 bugs reales más en el drawer móvil: flecha de "Productos" cortada y contenido corrido a la derecha (jul 2026)**: ambos por no cargar el preflight de Tailwind. (1) `<ul>` conservaba su `padding-inline-start: 40px` nativo del navegador — el reset existente solo le quitaba las viñetas, no el padding, así que cualquier `<ul>` con `w-full` quedaba 40px más angosto de lo esperado y empujaba el contenido a la derecha; se agregó `margin: 0; padding: 0;` a la regla `ul, ol` de `marketing-tailwind.css`. (2) el panel del drawer usa `box-sizing: content-box` por defecto (sin preflight), y combinado con su padding horizontal (`px-6`) el padding se sumaba al ancho declarado en vez de restarle espacio — el panel terminaba más ancho de lo previsto (comiéndose el espacio a la izquierda del rediseño anterior) y su contenido se desbordaba; se agregó `box-border` al contenedor del drawer en ambos navbars. Verificado con Playwright: `scrollWidth === clientWidth` del panel en `/` y `/delicatessen`, cero desbordamiento.
